export default function ResumenPage() {
  return (
    <div className="rounded-xl border bg-white p-6 text-sm text-neutral-500">
      Resumen — pendiente (milestone 4 del ROADMAP: tabla `infraruts` + KPIs +
      gráfico Recharts).
    </div>
  );
}
